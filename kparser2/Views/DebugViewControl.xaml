<UserControl x:Class="kparser2.Views.DebugViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Column="0"
                  ItemsSource="{Binding Packets}"
                  SelectedItem="{Binding SelectedPacket}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding PacketId, StringFormat=0x{0:X4}}" Width="80" />
                <DataGridTextColumn Header="Name" Binding="{Binding PacketName}" Width="*" />
                <DataGridTextColumn Header="Dir" Binding="{Binding Direction}" Width="70" />
                <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="60" />
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Column="1" Margin="8,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       Text="{Binding MetaSummary}"
                       TextWrapping="Wrap"
                       FontFamily="Consolas"
                       Margin="0,0,0,8" />
            <TextBox Grid.Row="1"
                     Text="{Binding HexDump, Mode=OneWay}"
                     FontFamily="Consolas"
                     IsReadOnly="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" />
        </Grid>
    </Grid>
</UserControl>
